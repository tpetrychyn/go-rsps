fmt := import("fmt")

if state == "bind" {
    doors := [1530]

    openDoorFunc := func(id) {
        fmt.printf("opened door %d \n", id)
        setObject(7399, 3232, 3234)
        //fmt.printf("target object %+v", object.Name)
        fmt.printf("%+v", player.Position)
    }

    for id in doors {
        bindObjectFirstClick(id, func() {openDoorFunc(id)})
    }
}

if state == "execute" {
    execute()
}
